
let numSelect = prompt("indicame un numero");

numSelect=parseInt(numSelect);

if (numSelect % 2 === 0 ) {
    alert(numSelect + " es divisible entre 2");

}else{
    alert(numSelect + " no es divisible entre 2")
}